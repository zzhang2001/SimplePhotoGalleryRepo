$(document).ready(function () {
    // Use jquery.matchHeight jQuery plugin to make the height of all columns equal.
    // The different column heights are caused by different image sizes.
    $('.eq-col').matchHeight();
});
